{
  "name": "portfolio-devops-assignment",
  "version": "1.0.0",
  "scripts": {
    "start": "echo 'No server needed for static html'",
    "build": "mkdir -p dist && cp src/index.html dist/index.html",
    "test": "echo 'All tests passed!'"
  }
}